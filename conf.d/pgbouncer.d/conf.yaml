init_config:

instances:
  - host: prod-pgbouncer-xuul
    port: 5432
    username: datadog
    password: ""   # if required
    tags:
      - env:production
      - service:pgbouncer

  - host: staging-pgbouncer-s0rr
    port: 5432
    username: datadog
    password: ""   # if required
    tags:
      - env:staging
      - service:pgbouncer